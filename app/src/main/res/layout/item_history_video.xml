<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:layout_margin="8dp"
    app:cardBackgroundColor="@color/cartoon_card_bg"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeColor="@color/cartoon_primary"
    app:strokeWidth="1dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp">

        <!-- Thumbnail with Play Button -->
        <ImageView
            android:id="@+id/video_thumbnail"
            android:layout_width="160dp"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/video_thumbnail"
            android:background="@drawable/cartoon_thumbnail_bg"/>

        <!-- Play Button Overlay -->
        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_centerVertical="true"
            android:layout_alignStart="@id/video_thumbnail"
            android:layout_marginStart="8dp"
            android:src="@drawable/ic_cartoon_play_small"
            app:tint="@android:color/white"/>

        <!-- Video Info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_toEndOf="@id/video_thumbnail"
            android:layout_toStartOf="@id/btn_remove"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:orientation="vertical"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/video_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/cartoon_text_dark"
                android:textSize="16sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"/>

            <TextView
                android:id="@+id/video_views"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/cartoon_text_light"
                android:textSize="14sp"
                android:layout_marginTop="8dp"/>
        </LinearLayout>

        <!-- Remove Button -->
        <ImageButton
            android:id="@+id/btn_remove"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_trash"
            android:contentDescription="@string/remove_from_history"
            app:tint="@color/cartoon_accent"/>
    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>