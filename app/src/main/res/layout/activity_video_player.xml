<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/constraint_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/cartoon_bg_gradient">

    <!-- YouTube Player Container with Cartoon Frame -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/youtube_container_card"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardBackgroundColor="@android:color/black"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp"
        app:strokeColor="@color/cartoon_primary"
        app:strokeWidth="2dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="16:9">

        <FrameLayout
            android:id="@+id/youtube_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:clipChildren="false">

            <com.pierfrancescosoffritti.androidyoutubeplayer.core.player.views.YouTubePlayerView
                android:id="@+id/youtube_player_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/black"/>

            <!-- Cartoon-style Fullscreen Button -->
            <ImageButton
                android:id="@+id/btn_fullscreen"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="bottom|end"
                android:layout_margin="16dp"
                android:background="@drawable/cartoon_circle_button"
                android:src="@drawable/ic_cartoon_fullscreen"
                android:contentDescription="@string/fullscreen"
                android:scaleType="fitCenter"
                app:tint="@android:color/white"/>
        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Video Info Card with Cartoon Style -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/video_info_card"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/youtube_container_card"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:cardBackgroundColor="@color/cartoon_card_bg"
        app:cardCornerRadius="16dp"
        app:cardElevation="6dp"
        app:strokeColor="@color/cartoon_primary"
        app:strokeWidth="1dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Title with Cartoon Font -->
            <TextView
                android:id="@+id/video_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/cartoon_text_dark"
                android:textSize="20sp"
                android:textStyle="bold"
                android:lineSpacingExtra="4dp"/>

            <!-- Info Row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingTop="8dp"
                android:paddingBottom="12dp">

                <TextView
                    android:id="@+id/video_views"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/cartoon_text_light"
                    android:textSize="14sp"
                    android:layout_marginEnd="16dp"/>

                <TextView
                    android:id="@+id/video_date"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/cartoon_text_light"
                    android:textSize="14sp"/>

                <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_weight="1"/>

                <!-- Cartoon-style Favorite Button -->
                <ImageButton
                    android:id="@+id/btn_favorite"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:src="@drawable/ic_cartoon_heart"
                    android:contentDescription="@string/favorite"
                    app:tint="@color/cartoon_primary"/>
            </LinearLayout>

            <!-- Description -->
            <TextView
                android:id="@+id/video_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/cartoon_text_dark"
                android:textSize="15sp"
                android:lineSpacingExtra="6dp"/>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Related Videos Header -->
    <TextView
        android:id="@+id/related_videos_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/related_videos"
        android:textColor="@color/cartoon_text_dark"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingStart="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/video_info_card"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Related Videos RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/related_videos_recycler"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="8dp"
        android:overScrollMode="never"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintTop_toBottomOf="@id/related_videos_header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- Floating Decoration -->
    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="16dp"
        android:src="@drawable/ic_cartoon_star_decoration"
        android:rotation="15"
        app:layout_constraintBottom_toTopOf="@id/related_videos_header"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="@color/cartoon_accent"/>
</androidx.constraintlayout.widget.ConstraintLayout>